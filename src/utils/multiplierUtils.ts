// import { Technology } from '@/types/technologies';

// interface Multipliers {
// clickMultiplier: number;
// autoMultiplier: number;
// }

// export function recalculateMultipliers(technologies: Technology[]): Multipliers {
// let newClickMultiplier = 1;
// let newAutoMultiplier = 0;

// technologies.forEach(tech => {
//     if (tech.unlocked) {
//     if (tech.type === 'click') {
//         newClickMultiplier += tech.income;
//     } else if (tech.type === 'auto') {
//         newAutoMultiplier += tech.income;
//     }
//     }
// });

// return { clickMultiplier: newClickMultiplier, autoMultiplier: newAutoMultiplier };
// }
